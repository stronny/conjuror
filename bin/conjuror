#!/usr/bin/env ruby

require('rubygems')
require('conjuror')

unless ARGV[0] then
	puts <<-EOH
Usage: #{basename $0} <incantation>
	EOH
	exit!(0)
end

incantation = YAML.load_file(ARGV[0])
Conjuror.invoke!(incantation)
